apiVersion: v1
kind: Service
metadata:
  name: fractal-agents
  namespace: ai
  labels:
    app: fractal-agents
spec:
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: fractal-agents